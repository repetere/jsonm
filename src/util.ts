// ts-node -O {\"module\":\"commonjs\"}  src/util.ts
import { monthNumbers, mockDates, } from './constants';

//   // weekNumbers,
//   // dayNumbers,
//   // quarterhourNumbers,
//   // ordinalDayNumbers,
//   // hourNumbers,
//   // minuteNumbers,
//   // secondNumbers,
//   mockDates,
//   // getPartialHour,
//   // getQuarterHour,
//   // getParsedDate,
//   // prettyTimeStringOutputFormat,
//   // timeProperty,
//   // dateTimeProperty,
//   // durationToDimensionProperty,
//   // featureTimeProperty,
//   // performanceValues,
// } from './constants';
// import { Info } from 'luxon';
// import chai from 'chai';
// import path from 'path';
// const expect = chai.expect;

// function generateNumberRange(start: number, end: number): number[]{
//   return [start].reduce((result:number[], val:number) => { 
//     for (let i=val; i < (end+1); i++){
//       result.push(i);
//     }
//     return result;
//   }, []);
// }
console.log({ monthNumbers,mockDates });


// import { ModelTrainningOptions } from './model';

// function testFunc(options: ModelTrainningOptions = {}) {
//   console.log('testing value of Options', options);
// }

// testFunc();
